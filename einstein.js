'use strict'

const myWeight = 65;
const speedLight = 3e8;

const einsteinEnergy = myWeight * Math.pow(speedLight, 2);

console.log(Math.pow(speedLight, 2));

console.log(`Энергия неопознанного тела ${einsteinEnergy} джоуль`);